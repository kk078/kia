# Cloudflare Pages config for the Secondary Brain frontend.
# Account is pinned via the CLOUDFLARE_ACCOUNT_ID env var / CI secret
# (Pages config files do NOT accept account_id).
name = "kia"
pages_build_output_dir = "dist"
compatibility_date = "2024-11-01"
