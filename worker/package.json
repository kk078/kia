{
  "name": "kia-api-proxy",
  "version": "0.1.0",
  "private": true,
  "description": "Cloudflare Worker edge proxy for the Secondary Brain backend",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^3.90.0"
  }
}
